# Инструкция по запуску проекта ChefMake

## 📋 Требования

Перед запуском проекта убедитесь, что у вас установлены:

- **Node.js** (версия 18 или выше)
- **npm** или **Yarn**
- **PostgreSQL** (можно использовать Supabase - бесплатный хостинг)
- **Redis** (можно использовать бесплатный хостинг, например Redis Cloud)

---

## 🚀 Пошаговая инструкция

### 1. Установка зависимостей

#### Backend

```bash
cd backend
npm install
```

#### Frontend

```bash
cd frontend
npm install
```

---

### 2. Настройка переменных окружения

#### Backend (.env файл)

Создайте файл `.env` в папке `backend` со следующим содержимым:

```env
# Порт сервера (по умолчанию 5000)
PORT=5000

# Подключение к PostgreSQL (Supabase)
POSTGRES_URI=postgresql://user:password@host:port/database?sslmode=require

# Redis настройки
REDIS_HOST=your-redis-host
REDIS_PORT=your-redis-port
REDIS_PASSWORD=your-redis-password

# JWT секретный ключ (используйте случайную строку)
JWT_SECRET=your-super-secret-jwt-key-here

# URL AI сервиса (Hugging Face)
HF_SPACE_BASE_URL=https://sidharthp2004-chefmate-llm-api.hf.space

# (Опционально) Telegram бот
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
```

**Как получить данные для подключения:**

1. **PostgreSQL (Supabase):**
   - Зарегистрируйтесь на [Supabase](https://supabase.com)
   - Создайте новый проект
   - Перейдите в Settings → Database
   - Скопируйте Connection String (URI)

2. **Redis:**
   - Можно использовать [Redis Cloud](https://redis.com/try-free/) (бесплатный план)
   - Или установить Redis локально
   - Для локального Redis: `REDIS_HOST=localhost`, `REDIS_PORT=6379`, `REDIS_PASSWORD=` (пусто)

3. **JWT_SECRET:**
   - Сгенерируйте случайную строку (можно использовать: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`)

#### Frontend (.env файл)

Создайте файл `.env` в папке `frontend` со следующим содержимым:

```env
# URL бэкенд API
VITE_API_BASE_URL=http://localhost:5000
```

---

### 3. Инициализация базы данных

После настройки подключения к базе данных, выполните SQL скрипты для создания схемы:

1. Откройте SQL редактор в Supabase (или используйте psql)
2. Выполните скрипты из папки `backend/sql/`:
   - `001_init_schema.sql` - создание базовой схемы
   - `002_alter_schema.sql` - дополнительные изменения схемы

Или используйте скрипт инициализации (если он есть):

```bash
cd backend
node init-db.js
```

---

### 4. Запуск приложения

#### Запуск Backend сервера

Откройте первый терминал:

```bash
cd backend
npm run dev
```

Сервер должен запуститься на `http://localhost:5000`

#### Запуск Frontend сервера

Откройте второй терминал:

```bash
cd frontend
npm run dev
```

Frontend должен запуститься на `http://localhost:8080`

---

### 5. Проверка работы

1. Откройте браузер и перейдите на `http://localhost:8080`
2. Вы должны увидеть главную страницу приложения ChefMake
3. Попробуйте зарегистрировать нового пользователя или войти

### 6. Телеграм-бот (опционально)

1. Создайте бота в Telegram через [@BotFather](https://t.me/BotFather) и получите токен.
2. Добавьте `TELEGRAM_BOT_TOKEN` в `backend/.env`.
3. Запустите бота:
   ```bash
   cd backend
   npm run telegram-bot
   ```
4. Доступные команды:
   - `/start` — инструкция
   - `/login email пароль` — вход в аккаунт ChefMake (пароль отправляется через Telegram, используйте команду в доверенной среде)
   - `/favorites` — список избранных
   - `/logout` — отключиться

После входа можно написать «Покажи избранные», номер или название рецепта, и бот пришлёт его описание.

---

## 🔧 Устранение неполадок

### Проблема: "Cannot connect to database"

**Решение:**
- Проверьте правильность строки подключения `POSTGRES_URI`
- Убедитесь, что база данных доступна и SSL включен
- Проверьте, что вы выполнили SQL скрипты для создания схемы

### Проблема: "Redis connection error"

**Решение:**
- Проверьте настройки Redis в `.env`
- Убедитесь, что Redis сервер запущен и доступен
- Если используете локальный Redis, запустите его: `redis-server`

### Проблема: "CORS error" в браузере

**Решение:**
- Убедитесь, что backend запущен на порту 5000
- Проверьте, что `VITE_API_BASE_URL` в frontend `.env` указывает на правильный адрес

### Проблема: "Module not found"

**Решение:**
- Убедитесь, что вы выполнили `npm install` в обеих папках (backend и frontend)
- Удалите `node_modules` и `package-lock.json`, затем выполните `npm install` снова

---

## 📝 Дополнительная информация

- **Backend API** работает на порту `5000` (по умолчанию)
- **Frontend** работает на порту `8080`
- **AI сервис** использует внешний Hugging Face Space
- Для продакшена настройте переменные окружения соответственно

---

## 🎯 Быстрый старт (краткая версия)

```bash
# 1. Установка зависимостей
cd backend && npm install
cd ../frontend && npm install

# 2. Создайте .env файлы (см. выше)

# 3. Запуск
# Терминал 1:
cd backend && npm run dev

# Терминал 2:
cd frontend && npm run dev

# 4. Откройте http://localhost:8080
```

---

Удачи с запуском проекта! 🚀

